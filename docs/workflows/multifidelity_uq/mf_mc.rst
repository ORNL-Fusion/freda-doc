Multi-fidelity Monte Carlo Analysis for Edge-Physics Models
==========

We investigate ...

----------------------

* **Fidelities**
  * C1 (low fidelity, ``fidelity1_c1``, approx. ? min per run, 1 core)
  * SOLPS (high fidelity, ``fidelity2_solps``, approx. ? min per run, 24 cores)
* **Physics parameters**
  * ...
* **Quantities of interest**
  * ...
* **Files**
  * ...

Running an Example
~~~~~~~~~~~~~~~~~~

Running an example involves a single call:

``ips_multifidelity_uq.py --config=sampling_sol.json​``

Generated Artifacts
~~~~~~~~~~~~~~~~~~~

  * ...
